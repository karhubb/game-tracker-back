-- Flyway migration: migrate existing notes JSON (if any) into the new notes table.
-- NOTE: This is a best-effort template. For complex JSON structures or large datasets,
-- prefer to write a bespoke migration script or run a one-off ETL job.

-- Example: if you previously stored notes as JSON in games.notes_json (TEXT),
-- you can parse and insert rows here. The exact SQL depends on your previous format.

-- Placeholder: manual migration recommended. See README for migration steps.
